type Query {
  lessonList: [Lesson!]!
}

type Mutation {
  addLesson(lesson: LessonInput!): Lesson
  addArticle(article: ArticleInput!, lessonId: Int!): Article
}

type Lesson {
  _id: ID!
  id: Int!
  lesson_title: String!
  sections: [Article!]
}

type Article {
  id: Int
  title: String!
  content: String!
}

input LessonInput {
  lesson_title: String!
}

input ArticleInput {
  title: String!
  content: String
}